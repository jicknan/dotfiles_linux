##
# mysql
# $Header$

= Initiazile MySQL =
 * mysql_install_db --user=mysql
 * mysqld_safe --user=mysql&
 * mysqladmin -u root -h 9JickNan1 password 'new-password'
 * mysql -u root -p


# vim:ft=text:
