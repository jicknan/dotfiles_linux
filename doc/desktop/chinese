##
# chinese - Using Chinese in Linux
#

= Document =
 * desktop/fonts
 * Using utf8 in Linux
   http://www.gentoo.org/doc/en/utf-8.xml
 *


= Display Chinese =
 * WenQuanYi Bitmap font
 * see fonts for config file setting

= Input Chinese =
 * scim+fcim
 * ~/.xinitrc
	export GTK_IM_MODULE="xim"
	export XMODIFIERS="@im=SCIM"
	scim -d 1> /dev/null 2> /dev/null

= Convert charset for file/dir name =
 $ convmv -f big5 -t utf8 -r --notest <dir>

= Convert charset for file content =
 $ iconv -f big5 -t utf-8 filename > newfile

# vim:ft=text:
